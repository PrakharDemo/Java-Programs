This folder has a program to demonstrate the Inner (Nested) class and object creation of nested class from Host class.
Also, we create the object of Parent class from Main() methiod, which can be inside or outside the Host class.
